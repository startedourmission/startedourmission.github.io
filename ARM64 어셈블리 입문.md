## 환경

- Mac Apple Silicon (ARM64)
- 어셈블러: `as` (Xcode Command Line Tools 내장)
- 확장자: `.s`
- 편집: vim

## 빌드/실행

```bash
as -o 파일.o 파일.s && ld -o 파일 파일.o -lSystem -syslibroot $(xcrun --sdk macosx --show-sdk-path) -e _main -arch arm64 && ./파일
```

- `as`: 어셈블 (소스 -> 기계어)
- `ld`: 링크 (실행파일 만들기). macOS는 `-lSystem`(시스템 라이브러리), `-syslibroot`(SDK 경로) 등이 필요해서 길다. 리눅스는 `ld -o out out.o`면 끝.
- `xcrun --sdk macosx --show-sdk-path`: SDK 경로를 자동으로 찾아주는 셸 명령어

## 프로그램 구조

```asm
.global _main       // 시작점을 외부에 공개
.align 4

.data                // 데이터 영역 (문자열, 숫자 등)
msg: .asciz "hello"

.text                // 코드 영역 (실행할 명령어)
_main:               // 프로그램 시작점
    // 명령어들 ...
```

- `.data`: 데이터 영역. 라벨로 이름 붙이고 데이터를 선언
- `.text`: 코드 영역. CPU가 위에서 아래로 순서대로 실행
- `_main:`: 프로그램 시작점. OS가 제일 먼저 찾는 이름
- `.global _main`: 링커에게 시작점 알려주기

## 레지스터

CPU 칩 안에 있는 물리적 저장 공간. RAM보다 훨씬 작고 빠르다.

- `x0` ~ `x28`: 범용 레지스터 29개, 각 64비트
- `w0` ~ `w28`: 같은 레지스터의 하위 32비트 버전. `strb` 같은 바이트 연산에 사용
- `x29` (FP): 프레임 포인터
- `x30` (LR): 리턴 주소
- `SP`: 스택 포인터
- `PC`: 현재 실행 중인 명령어 위치

CPU는 메모리에서 직접 계산 못 한다. 레지스터에 가져와서 계산하고 다시 메모리에 저장하는 방식.

## 데이터 선언

`.data` 아래에서 사용. 라벨은 메모리 주소에 붙인 이름.

| 지시어 | 단위 | 범위 | 예시 |
|--------|------|------|------|
| `.byte` | 1바이트 | 0~255 | `.byte 65` |
| `.word` | 4바이트 | | `.word 1000` |
| `.quad` | 8바이트 | | `.quad 999999` |
| `.asciz` | 문자열 + \0 | | `.asciz "hello"` |
| `.ascii` | 문자열 (\0 없음) | | `.ascii "hello"` |
| `.space` | N바이트 공간 확보 | | `.space 16` |

```asm
msg: .asciz "hello"     // 문자열 (끝에 \0 자동 추가)
num: .byte 0             // 1바이트, 초깃값 0
buf: .space 2            // 2바이트 빈 공간
vals: .byte 10, 20, 30   // 3바이트, 값 3개
```

- `asciz` = ASCII + Zero. 끝에 `\0` 붙여줌
- 어셈블리에는 자료형이 없다. 전부 바이트 덩어리일 뿐, 해석은 프로그래머 몫

## 핵심 명령어

| 명령어 | 의미 | 예시 |
|--------|------|------|
| `mov` | 값 넣기 | `mov x0, #5` / `mov x0, x1` |
| `add` | 더하기 | `add x2, x0, x1` (x2 = x0 + x1) |
| `sub` | 빼기 | `sub x2, x0, x1` |
| `udiv` | 나눗셈 (몫) | `udiv x1, x0, x2` (x1 = x0 / x2) |
| `msub` | 나머지 계산 | `msub x3, x1, x2, x0` (x3 = x0 - x1*x2) |
| `strb` | 레지스터 -> 메모리 (1바이트) | `strb w3, [x4]` |
| `ldrb` | 메모리 -> 레지스터 (1바이트) | `ldrb w3, [x4]` |
| `adrp` | 라벨 주소 가져오기 (페이지) | `adrp x4, num@PAGE` |
| `svc` | 시스템 콜 호출 | `svc #0x80` |
| `b` | 점프 | `b my_func` |

### 즉시값과 레지스터

- `#5`: 숫자 5 그 자체 (즉시값)
- `x1`: x1 레지스터에 들어있는 값

```asm
mov x0, #5      // x0에 숫자 5를 넣어라
mov x0, x1      // x0에 x1의 값을 복사해라
```

### 메모리 접근

- `[x4]`: x4가 가리키는 메모리 위치
- `[x4, #1]`: x4 주소에서 1바이트 뒤 (배열 인덱스처럼)

```asm
strb w3, [x4]       // arr[0]에 저장
strb w5, [x4, #1]   // arr[1]에 저장
```

## 주소 가져오기 (adrp + add)

ARM64 명령어는 전부 4바이트(32비트) 고정인데, 메모리 주소는 64비트라 한 번에 못 담는다. 그래서 두 단계로 나눠서 가져온다.

```asm
adrp x4, num@PAGE       // 페이지(큰 단위) 주소
add  x4, x4, num@PAGEOFF // 페이지 안에서 정확한 위치
```

- `@PAGE`: 주소의 앞부분 (큰 구역)
- `@PAGEOFF`: 주소의 뒷부분 (구역 안 위치)
- 어셈블러가 빌드할 때 알아서 쪼개준다. 직접 계산할 필요 없음
- 라벨 주소 가져올 때 항상 이 두 줄 세트

## 시스템 콜

OS에게 뭔가 해달라고 요청하는 것. 프로그램은 직접 화면 출력이나 파일 접근을 못 하고 OS를 거쳐야 한다.

패턴: 레지스터에 세팅 -> `svc #0x80` 호출

| 레지스터 | 역할 |
|---------|------|
| `x16` | 시스템 콜 번호 (뭘 할지) |
| `x0` | 첫 번째 인자 |
| `x1` | 두 번째 인자 |
| `x2` | 세 번째 인자 |

### 자주 쓰는 시스템 콜

| x16 값 | 이름 | 인자 |
|---------|------|------|
| 1 | exit | x0 = 종료 코드 |
| 3 | read | x0 = fd, x1 = 버퍼 주소, x2 = 크기 |
| 4 | write | x0 = fd, x1 = 데이터 주소, x2 = 크기 |
| 5 | open | x0 = 경로 주소 |
| 6 | close | x0 = fd |

### 파일 디스크립터

| 번호 | 이름 | 의미 |
|------|------|------|
| 0 | stdin | 키보드 입력 |
| 1 | stdout | 화면 출력 |
| 2 | stderr | 에러 출력 |

### write 예시

```asm
mov  x0, #1              // stdout (화면)
adrp x1, msg@PAGE        // 출력할 데이터 주소
add  x1, x1, msg@PAGEOFF
mov  x2, #6              // 바이트 수
mov  x16, #4             // write 시스템 콜
svc  #0x80               // 호출
```

### exit 예시

```asm
mov  x0, #0       // 종료 코드 0 (정상)
mov  x16, #1      // exit 시스템 콜
svc  #0x80
```

exit를 안 쓰면 CPU가 코드 끝을 지나 쓰레기 메모리를 실행하다 터진다. 항상 넣을 것.

## 숫자 출력

어셈블리에는 `print` 같은 게 없다. 숫자를 출력하려면:

1. 숫자에 48을 더해 ASCII 문자로 변환 (한 자릿수: 0~9)
2. 메모리에 저장 (`strb`)
3. write 시스템 콜로 출력

```asm
add x3, x3, #48     // 숫자 3 -> ASCII '3' (51)
strb w3, [x4]       // 메모리에 저장
// write로 출력
```

두 자릿수는 십의 자리와 일의 자리를 따로 계산해야 한다:

```asm
// 15를 출력하려면
udiv x5, x3, x7      // 15 / 10 = 1 (몫)
msub x6, x5, x7, x3  // 15 - 1*10 = 5 (나머지)
add  x5, x5, #48     // '1'
add  x6, x6, #48     // '5'
strb w5, [x4]        // 첫 번째 바이트
strb w6, [x4, #1]    // 두 번째 바이트
// write에서 x2 = #2로 2바이트 출력
```

## ARM64 vs x86-64

| | ARM64 (Apple Silicon) | x86-64 (Intel/AMD) |
|---|---|---|
| 명령어 크기 | 고정 4바이트 | 가변 1~15바이트 |
| 레지스터 이름 | x0~x28 (29개) | rax, rbx 등 (16개) |
| 철학 | RISC (단순하고 빠르게) | CISC (복잡하지만 한 번에 많이) |
| add | `add x2, x0, x1` (3개) | `add rax, rbx` (2개, 결과 덮어쓰기) |
| 시스템 콜 | `svc #0x80` | `syscall` |

## 기타

- 들여쓰기: 문법적으로 필요 없지만, 관례상 라벨은 맨 왼쪽, 명령어는 들여쓰기
- 주석: `//`
- 프로그램 끝을 나타내는 문법은 없다. exit 시스템 콜로 종료
- 잘못된 메모리 접근 시 Segmentation Fault 발생. OS가 해당 프로그램만 죽이고 컴퓨터는 무사
- OS가 프로그램마다 가상 메모리로 영역을 분리하기 때문에 다른 프로그램이나 시스템에 영향 없음

## 실습 파일

- `~/Dev/assem/hello.s`: Hello World (문자열 출력)
- `~/Dev/assem/add.s`: 덧셈 결과 출력
